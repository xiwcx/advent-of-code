# Advent of Code — Monorepo

Each year is a separate sub-project under its own directory (`2022/`, `2024/`, `2025/`, etc.), potentially using different languages.

When working within a year's directory, invoke Claude from that directory (e.g. `cd 2025 && claude`) so the year-specific `CLAUDE.md` is loaded alongside this file.
